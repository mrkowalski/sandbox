schema: spec-driven
context: |
  Keep all generated artifacts terse. No boilerplate sections, no restating
  obvious context. proposal.md: 3-5 sentences max. specs: only scenarios
  for non-obvious behavior, skip trivial CRUD-style ones. Skip design.md
  entirely unless the change touches architecture or has real tradeoffs.

